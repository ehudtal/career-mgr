<h1><%= @employer.name %></h1>

<h2>Industries</h2>
<%= render 'industries/list', object: @employer %>

<h2>Opportunities</h2>
<%= render "opportunities/list", opportunities: @employer.opportunities %>
<br />
<%= link_to 'Add Opportunity', new_employer_opportunity_path(@employer), class: 'button' %>

<h2>Locations</h2>
<%= render "locations/list", locations: @employer.locations %>
<br />
<%= link_to 'Add Location', new_employer_location_path(@employer), class: 'button' %>

<footer class="actions">
  <%= link_to 'Edit', edit_employer_path(@employer), class: 'edit' %> |
  <%= link_to 'Back to All Employers', employers_path, class: 'back' %>
</footer>

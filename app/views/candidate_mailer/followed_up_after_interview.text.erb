Have you followed up after your interview with <%= @opp.employer.name %>?

Send a quick follow up to express your continued excitement for the opportunity and to follow-up on anything from the interview.

Yes:
<%= @token.path_with_token('followed up after interview') %>

Employer Declined:
<%= @token.path_with_token('declined') %>

Not Yet:
<%= @token.path_with_token('no change') %>

Does Not Apply:
<%= @token.path_with_token('skip') %>

No Longer Interested:
<%= @token.path_with_token('not interested') %>

<h1>Editing Opportunity</h1>

<%= render 'form', employer: @employer, opportunity: @opportunity %>

<%= link_to 'Show', opportunity_path(@opportunity) %> |
<%= link_to 'Back', employer_opportunities_path(@opportunity.employer) %>
